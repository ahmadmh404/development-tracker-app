# PostgreSQL Database Configuration
# Used by docker-compose.yml and Drizzle ORM

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_NAME=project_tracker

# Full connection string (constructed from above values)
DATABASE_URL=postgresql://postgres:your_secure_password_here@localhost:5432/project_tracker
